<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistema JESSE</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body { background-color: #f4f6f9; }
        .navbar { background-color: #2c3e50; }
        .card { border: none; shadow: 0 4px 6px rgba(0,0,0,0.1); }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark mb-4">
  <div class="container">
    <a class="navbar-brand" href="/">💪 GIMNASIO JESSE</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto align-items-center">
        
            <li class="nav-item">
                <a class="nav-link" href="/"><i class="fas fa-home"></i> Inicio</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="/registrar"><i class="fas fa-user-plus"></i> Nuevo Socio</a>
            </li>

            <li class="nav-item mx-2 text-white-50">|</li>

            <% if (esAdmin) { %>
                <li class="nav-item">
                    <a class="btn btn-warning btn-sm fw-bold me-2" href="/admin">
                        <i class="fas fa-tools"></i> Panel Admin
                    </a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-danger btn-sm" href="/logout">
                        <i class="fas fa-sign-out-alt"></i> Salir
                    </a>
                </li>
            <% } else { %>
                <li class="nav-item">
                    <a class="btn btn-outline-light btn-sm" href="/login">
                        <i class="fas fa-lock"></i> Admin
                    </a>
                </li>
            <% } %>

        </ul>
    </div>
    </div>
</nav>

<div class="container">